# Large regenerable data files (paper: "regenerate with npm run stats:all")
results/split-scores.json
results/divergence.json
results/divergence-matrix.csv
results/figures/

# Raw response data (append-only, regenerable)
data/runs/*/responses.jsonl
data/runs/*/failures.jsonl

# Derived data (regenerable)
data/derived/normalized.jsonl
data/derived/normalization-report.json

# Probe/verify run artifacts (regenerable)
data/probe-*/
data/verify-*/

# OS
.DS_Store
Thumbs.db

# Node
node_modules/